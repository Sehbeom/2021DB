<h1>{{ title }}</h1>
<table>
    <tr>
        <td>Dname</td>
        <td>Dnumber</td>
    </tr>
    {{#each department}}
    <form method="post">
        <tr>
            <td>{{Dname}}</td>
            <td>{{Dnumber}}</td>
            <td style="border: none; margin-left: 10px;">
                <button style="margin-left: 10px;" name='delBtn' type="submit" value="{{Dnumber}}" formaction="/delete">
                    삭제
                </button>
            </td>
        </tr>
    </form>
    {{/each}}
</table>
<br>
<br>
<table>
    <tr>
        <td>Subject_name</td>
        <td>Subject_id</td>
    </tr>
    {{#each subject}}
    <form method="post">
        <tr>
            <td>{{subject_name}}</td>
            <td>{{subject_id}}</td>
            <td style="border: none; margin-left: 10px;">
                {{!-- <button style="margin-left: 10px;" name='delBtn2' type="submit" value="{{subject_id}}" formaction="/delete">
                    삭제
                </button> --}}
                <button style="margin-left: 10px;" name='delBtn2' type="submit" value="{{subject_name}}" formaction="/delete">
                    삭제
                </button>
            </td>
        </tr>
    </form>
    {{/each}}
</table>